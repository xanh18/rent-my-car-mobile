package com.example.rentmycar

import com.example.rentmycar.adapter.TripAdapter
import com.example.rentmycar.model.Trip
import org.junit.Test


import org.junit.Assert.*
import org.junit.Before
import org.mockito.Mockito
import org.mockito.Mockito.mock
import java.lang.Exception


/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
//
//    lateinit var adapter: TripAdapter
//
//    @Before
//    @Throws(Exception::class)
//    fun setUp() {
//        adapter = mock(TripAdapter::class.java)
//        Mockito.`when`(adapter.hasStableIds()).thenReturn(true)
//    }
//
//    @Test
//    fun updateTrips() {
//        var tripListMutable: MutableList<Trip> = mutableListOf()
//        tripListMutable.add(Trip(1, null, null, null, null, null, null, null))
//        tripListMutable.add(Trip(2, null, null, null, null, null, null, null))
//        tripListMutable.add(Trip(3, null, null, null, null, null, null, null))
//        var tripList: List<Trip> = tripListMutable
//        adapter.update(tripList)
//        var amount = adapter.itemCount
//        assertEquals(3, amount)
//    }
}